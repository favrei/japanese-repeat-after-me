# Agent Tools Index

Standalone helper scripts for repeated project commands belong here.

This directory is an isolated `uv` project. Run a registered tool from this
directory with:

```bash
uv run <script-name>
```

Add each command to `[project.scripts]` in `pyproject.toml` and document its
purpose, inputs, outputs, and replay example below.

## Registered tools

None yet.
