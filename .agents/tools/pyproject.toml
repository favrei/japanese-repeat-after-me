[project]
name = "japanese-repeat-after-me-agent-tools"
version = "0.1.0"
description = "Standalone helper tools for the Japanese Repeat-After-Me workspace"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
