# Office-gig preview host stopped

The local production preview at `http://localhost:3000/` was stopped at the
user's request after the office-gig review. A connection check confirmed that
port 3000 no longer serves the app.
