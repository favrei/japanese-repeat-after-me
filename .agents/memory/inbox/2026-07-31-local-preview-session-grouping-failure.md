# Local preview browser session naming could not group tabs

- Naming the Chrome browser session for the local AirPods test failed because the selected Chrome window does not support tab grouping.
- This matches the earlier Sites handoff limitation and happened before the new local tab was created.
- The recovery follows the Browser guidance: create an ungrouped tab, navigate it to the exact local URL, and keep it as the user deliverable.
