<!--
DURABLE MEMORY — HARD WRITE BOUNDARY
Do not edit this file unless the user explicitly asked to dream/consolidate or
directly requested a durable-memory edit. During normal work, write only under
.agents/memory/inbox/.
-->

# Platform

Canonical memory for development environment, browser targets, PWA behavior,
runtime capability tiers, and hosting assumptions.

## Current targets

- Development and first validation: local Apple M3 MacBook with macOS Chrome.
- Primary runtime target: Android Chrome.
- Optional targets: Linux AMD64 Chrome and iOS Chrome or installed PWA, each
  requiring separate validation.
- Intended hosting target: GPT Sites.
- A private GPT Sites test kit has demonstrated authenticated API routes and
  server-side recording storage/export. The intended application may still be
  static or mostly static; this evidence does not establish that its core loop
  requires a backend.

## Browser and PWA expectations

The intended browser architecture may use:

- React, TypeScript, and Vite;
- Web Audio API and AudioWorklet;
- Web Workers;
- WebAssembly;
- IndexedDB or OPFS;
- Service Worker;
- optional WebGPU.

The PWA should support installation where available, offline lesson access
after download, local progress, separately cached recognition models, an
explicit model-download flow, recovery after browser cache eviction, and clear
microphone-permission handling.

Heavier audio and recognition work should run outside the main UI thread.

Candidate interaction state:

```text
idle
→ playing reference
→ recording
→ evaluating
→ passed or failed
→ next repetition
```

Candidate responsibility boundary:

```text
UI
→ audio capture
→ runtime adapter
→ recognition output
→ scoring engine
→ feedback visualization
```

## Device constraints

### macOS Chrome

- Expected capabilities include microphone access, AudioWorklet, workers,
  WebAssembly, persistent browser storage, service workers, PWA installation,
  and WebGPU on supported configurations.
- M3 performance is the first baseline but must not be treated as
  representative of mobile hardware.

### Android Chrome

Important unknowns include variable memory limits, thermal throttling, model
download and initialization time, device-dependent sample rates, microphone
interruptions, tab suspension, limited background execution, and storage
eviction.

### Linux Chrome

Likely standards-compatible, but microphone permissions/devices, Japanese
system voices, WebGPU drivers, PWA behavior, and storage quotas remain untested.

### iOS

Chrome on iOS must not be assumed to behave like desktop or Android Chrome.
WebAssembly performance, WebGPU, AudioWorklet, microphone capture, service
workers, persistent storage, model caching, memory limits, and PWA lifecycle
all require separate checks. A limited local tier or explicitly optional remote
scorer may be needed, but no remote dependency is currently assumed.

## Capability-tier hypothesis

- Tier A: WebGPU and sufficient memory/storage; full local recognizer plus
  acoustic alignment and visualization.
- Tier B: WASM CPU and sufficient memory for a small recognizer; reduced
  concurrency or slower evaluation.
- Tier C: insufficient recognizer resources; basic recording quality, timing,
  and reference comparison without detailed pronunciation claims.

Runtime capability detection is preferred over device-name checks. The selected
tier should be communicated clearly.

## Verified GPT Sites recording behavior

The private **Japanese Voice V1 Test Kit** supplied limited hosting evidence on
2026-07-29 and 2026-07-30:

- a live browser uploaded WebM recording objects through
  `POST /api/recordings`;
- the server listed recordings by session and returned individual objects;
- `GET /api/recordings?session=...&export=tar` returned a dataset archive;
- ChatGPT sign-in protected the site;
- session identity was browser/device-local enough that a later Chrome login
  opened a new empty session while the earlier server-side data remained.

This proves a private recording-test backend can operate on GPT Sites. It does
not prove stable AudioWorklet PCM capture, offline/PWA behavior, recognition,
large-model delivery, or final-product privacy policy. See
[Recordings](recordings.md).

## Remaining unverified GPT Sites assumptions

- static asset size limits;
- efficient large-model delivery;
- cache-control behavior;
- MIME types for WebAssembly and model files;
- configurable cross-origin isolation headers;
- service-worker support;
- deployment and cached-update behavior.

## Active platform gates

- The user has deferred device-deployment work while model and pipeline
  functionality and recall/precision are evaluated.
- Preserve the incomplete local experiment 005 benchmark under
  `experiments/005-device-runtime-benchmark/`; do not build, deploy, or treat it
  as current evidence until the user reprioritizes this lane.
- When deployment work resumes, confirm stable microphone PCM capture, a usable
  WASM CPU baseline, optional WebGPU behavior, representative Android resource
  limits, and GPT Sites model delivery/caching before fixing the runtime
  architecture.

## Future deployment direction

- Train or fine-tune privately on the M3 with a Metal-capable framework if
  model evidence warrants it, then compare quantized artifacts against an
  unquantized baseline.
- Keep private recordings and training-only artifacts out of deployed
  packages.
- Ship the application separately from a versioned model download and cache it
  for offline reuse.
- Retain tested WASM CPU compatibility and treat WebGPU as capability-detected
  acceleration.
- Candidate paths such as MLX or PyTorch MPS for local training and
  ONNX/WebGPU/WASM for deployment remain evidence-gated, not selected.

## Sources

- `.agents/documents/platform-scope.md`
- `.agents/documents/open-questions.md`
- `README.md`
- `experiments/002-browser-microphone-capture/README.md`
- [Recordings](recordings.md)

## Related cells

- [Project](project.md)
- [Recognition](recognition.md)
- [Experiments](experiments.md)
- [Recordings](recordings.md)
