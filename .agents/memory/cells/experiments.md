<!--
DURABLE MEMORY — HARD WRITE BOUNDARY
Do not edit this file unless the user explicitly asked to dream/consolidate or
directly requested a durable-memory edit. During normal work, write only under
.agents/memory/inbox/.
-->

# Experiments

Canonical memory for cloud-era experimental evidence, negative results,
reproducibility gaps, and required local continuation.

## Evidence boundary

- Cloud experiments ran on 2026-07-30.
- Standard Node and Python code could run in the Linux execution container.
- Browser navigation was blocked by administrator policy.
- External Vosk package and model downloads were unavailable.
- Therefore the cloud evidence covers deterministic algorithm spikes, not real
  Japanese learner audio, microphone capture, browser recognition, or M3/mobile
  performance.
- Environment blocks are not negative findings about Chrome microphone capture
  or Vosk quality.

## 001 — Synthetic acoustic DTW

Question: can dependency-free JavaScript align short acoustic sequences,
tolerate moderate rate variation, and localize synthetic changes?

Implementation:

- `experiments/001-browser-dtw-synthetic/dtw.js`
- Recorded on Linux x64 with Node v22.16.0.
- Eight synthetic tone units, 16 kHz sample rate, 320-sample frame,
  160-sample hop.
- Features are normalized log RMS plus hand-selected frequency magnitudes.
- Cosine frame distance and DTW with `0.03` insertion/deletion penalty.

Recorded result:

| Case | Normalized cost | Worst reference unit |
|---|---:|---:|
| same with noise | 0.087370 | 7 |
| slower, crude pitch-preserving stretch | 0.138072 | 3 |
| slower, pitch-shifted resampling | 0.298517 | 7 |
| missing unit 3 | 0.142920 | 3 |
| wrong unit 4 | 0.211614 | 4 |
| extra pause after unit 2 | 0.186852 | 7 |

Belief:

- The spike localized the deliberately missing and replaced units.
- A rate change that retained tone frequencies remained relatively low-cost.
- Pitch-shifted resampling caused a false high error.
- Plain DTW failed to localize the inserted pause because path stretching can
  hide extra time.
- DTW and local-cost visualization are cheap enough to prototype, but real
  speech features, explicit pause/duration evidence, varied voices, and learner
  recordings are still required.
- This result does not establish Japanese pronunciation quality or thresholds.

Repository gap:

- `experiments/README.md` references a nonexistent `run_local.py`.
- The experiment is described as a Chrome question, but the committed evidence
  only demonstrates Node execution.

Evidence:

- `experiments/001-browser-dtw-synthetic/results/2026-07-30-container-node.json`
- `experiments/001-browser-dtw-synthetic/README.md`

## 002 — Browser microphone capture

Question: can Chrome produce stable PCM frames suitable for local scoring?

Result:

- Blocked before microphone acquisition because administrator policy rejected
  headless Chromium navigation in the cloud container.
- `getUserMedia` and AudioWorklet were not tested.

Repository gap:

- Only a README is committed; there is no page, automated harness, or
  AudioWorklet implementation to serve.
- The experiment must not be treated as complete until a real harness records
  permission outcome, sample rate, frames, duration, RMS range, and repeated
  play/record stability on macOS Chrome.

Evidence:

- `experiments/002-browser-microphone-capture/README.md`

## 003 — Kana/mora alignment

Question: can deterministic alignment localize visible kana insertions,
deletions, and substitutions?

Implementation:

- `experiments/003-kana-mora-alignment/align.py`
- Standard Python unit-cost edit-distance dynamic programming.
- Joins small kana `ゃゅょぁぃぅぇぉゎ` to the preceding character.
- Keeps small `っ` separate so a missing geminate marker is visible.

Recorded result:

- exact sentence: distance `0`;
- missing `う` in `きょう`: deletion, distance `1`;
- missing small `っ`: deletion, distance `1`;
- extra `は`: insertion, distance `1`;
- `たべます` versus `たべません`: insertion plus substitution, distance `2`.

Belief:

- The baseline is sufficient to visualize simple differences after a
  recognizer emits kana.
- It is not pronunciation assessment and cannot separate recognition error from
  pronunciation error.
- It lacks katakana, alternative long-vowel, numeral, kanji-reading, broad
  punctuation, and ASR-token normalization.

Evidence:

- `experiments/003-kana-mora-alignment/results/2026-07-30-container-summary.json`
- `experiments/003-kana-mora-alignment/README.md`

## 004 — Vosk Japanese local recognition

Question: can `vosk-model-small-ja-0.22` provide a useful local
sentence-verification signal on the M3 and later in Chrome/WASM?

Cloud result:

- Setup blocked because Vosk and the model were unavailable and downloads were
  disabled.
- No accuracy, latency, memory, or learner-feedback claim exists.

Committed local harness:

- `run_macos.sh` creates an experiment-local venv, installs Vosk, downloads the
  model, and calls `transcribe.py`.
- `transcribe.py` requires mono 16-bit PCM WAV, enables word details, reads
  4,000-frame chunks, and outputs JSON segments.

Future recorded runs should retain expected text and kana, recognized text,
word confidence/timestamps, wall-clock latency, peak process memory, audio
duration/device, and the result of the proposed pass rule.

Gaps:

- It tests native Python Vosk, not browser/WASM Vosk.
- It does not measure wall-clock time or peak memory itself.
- No audio corpus or M3 result is committed.
- A good transcript would be a content signal, not proof of pronunciation
  quality.
- The harness predates the repository's `uv` rule and uses `venv` plus pip.
- `experiments/requirements.txt` contains comments only; experiments 001 and
  003 need standard runtimes, while experiment 004 installs Vosk itself.

Evidence:

- `experiments/004-vosk-japanese-local/README.md`
- `experiments/004-vosk-japanese-local/run_macos.sh`
- `experiments/004-vosk-japanese-local/transcribe.py`

## Highest-value next evidence

1. Repair experiment 001 replay documentation.
2. Implement and run a real experiment 002 capture harness in macOS Chrome.
3. Measure native Vosk accuracy, latency, and memory with real Japanese audio
   on the M3.
4. Repeat resource and microphone measurements on representative Android
   devices.
5. Test real-speech acoustic alignment across speakers and rates.
6. Gather human-labeled learner attempts for false accept/reject behavior and
   feedback usefulness.
7. Verify GPT Sites model delivery and PWA requirements.

These are evidence checkpoints, not approved product requirements.

## Related cells

- [Project](project.md)
- [Platform](platform.md)
- [Recognition](recognition.md)
