<!--
DURABLE MEMORY — HARD WRITE BOUNDARY
Do not edit this file unless the user explicitly asked to dream/consolidate or
directly requested a durable-memory edit. During normal work, write only under
.agents/memory/inbox/.
-->

# Recordings

Canonical memory for recorded-audio provenance, storage, recovery, local
availability, privacy, and use as experimental evidence.

## Current belief

- Real user recordings exist in a private GPT Sites test session, but no local
  copy exists as of 2026-07-30.
- The recordings are server-side objects, not data held only in a browser
  profile. The live application supports upload, session listing, individual
  object reads, and tar export through `/api/recordings`.
- A fresh browser or device may create a new empty session. An empty progress
  display therefore does not establish that an earlier session was deleted.
- Stored objects use WebM filenames organized by sentence and take. The archive
  has not yet been downloaded or inventoried locally, so its exact take count,
  durations, codecs, and recording conditions remain unverified.
- These recordings can become a first private learner-speech corpus, but their
  existence alone is not evidence of recognition accuracy, pronunciation
  quality, or AudioWorklet PCM stability.
- The temporary macOS Kyoko TTS sample used to smoke-test Vosk was not the
  user's voice and was deleted with its temporary replay directory.

## Private GPT Sites session

- Site: **Japanese Voice V1 Test Kit**
- URL: `https://japanese-voice-v1-test.zenridge.chatgpt.site`
- Sites project ID: `appgprj_6a69eeb7241c8191b4d406ed063ce168`
- Recorded session: `peter-v1-20260729-v9vatj`
- Later empty session: `peter-v1-20260730-kgb59v`

Production worker logs inspected on 2026-07-30 showed:

- successful recording uploads: `POST /api/recordings`, status `201`;
- successful session reads: `GET /api/recordings?session=...`, status `200`;
- successful archive exports:
  `GET /api/recordings?session=...&export=tar`, status `200`.

## Fast recovery

While authenticated with the owning ChatGPT account, download:

`https://japanese-voice-v1-test.zenridge.chatgpt.site/api/recordings?session=peter-v1-20260729-v9vatj&export=tar`

If the session ID is unknown:

1. Check `.openai/hosting.json`; this repository had no Sites metadata on
   2026-07-30.
2. List owned and editable Sites, using a limit no greater than `50`.
3. Select **Japanese Voice V1 Test Kit** and copy its opaque project ID
   exactly.
4. Read production worker logs with `errors_only: false`, `limit: 100` or less,
   and a time window covering the recording date.
5. Find `POST /api/recordings`, `GET ...?session=...`, or
   `GET ...?session=...&export=tar` requests and copy the session ID verbatim.
6. Use the authenticated export URL above.

The application also supports `GET /api/recordings?sessions=1`. Direct
navigation to that route was blocked by the Chrome control client during the
2026-07-30 recovery, so worker logs were the reliable discovery path.

## Handling rules

- Do not start a new session when the goal is recovery.
- Do not persist or expose Sites bypass tokens, cookies, signed headers, or
  other credentials. Use the Sites connector or an authenticated browser.
- Download the private archive only when the user requests it.
- Keep raw private recordings and derived audio out of Git unless the user
  explicitly approves a redistributable fixture.
- After download, preserve the raw archive, record its checksum and provenance,
  inventory takes and codecs, and derive 16 kHz mono PCM WAV files separately
  for native recognizer tests.
- If direct authenticated export fails, the original cloud Codex session can
  call the same endpoint; prior cloud export requests succeeded.
- Preserve useful session IDs in this cell because worker-log retention is
  finite.

## Open questions

- How many usable takes does the archive contain?
- Which device, microphone path, room, and speaking conditions produced each
  take?
- May the private recordings be retained as a repeatable local evaluation
  corpus, and for how long?
- Which recordings should receive expected-text, kana, and human-quality
  labels before recognizer evaluation?

## Sources

- GPT Sites production worker logs inspected on 2026-07-30.
- Live **Japanese Voice V1 Test Kit** interface inspected on 2026-07-30.
- `experiments/004-vosk-japanese-local/README.md`

## Related cells

- [Experiments](experiments.md)
- [Platform](platform.md)
- [Recognition](recognition.md)
- [Product](product.md)
