# Parked Sites experiment is preserved in its own local Git repository.
/experiments/005-device-runtime-benchmark/

# Isolated product PoC is preserved in its own local Git repository.
/poc/

# Private voice corpus stays local and is never pushed to the public origin.
/datasets/

# TTS auditions are disposable; accepted, provenance-tracked story clips remain trackable.
/stories/*/audio/candidates/
